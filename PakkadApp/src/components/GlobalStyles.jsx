// ไฟล์รวม global style
import { createGlobalStyle } from "styled-components";

export const GlobalStyle = createGlobalStyle`
    :root {
        --mainColor: #20803B;
        --subColor: #F1F3F6;
        --mainTextColor: #000000;
        --subTextColor: #FFFFFF;
        
    }
`